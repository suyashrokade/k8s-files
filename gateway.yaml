apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: suyash-gateway
  namespace: suyash-ns
spec:
  gatewayClassName: kgateway
  listeners:
    - name: http
      port: 80
      protocol: HTTP
      allowedRoutes:
        namespaces:
          from: Same